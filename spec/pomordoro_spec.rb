require 'spec_helper'

describe Pomordoro do
  it 'has a version number' do
    expect(Pomordoro::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
